# Safe to expose to the browser — protected by Row Level Security, not a secret.
# Solo se usa para refrescar la cookie de sesión en proxy.ts; el resto de las
# consultas a Supabase pasan por el backend (ver NEXT_PUBLIC_API_URL).
NEXT_PUBLIC_SUPABASE_URL=https://zywkijtfcitusocfrpaf.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_ku97fUAMZGDI-U03JY0jwg_0Fny8raW

# URL del backend (Luck/backend)
NEXT_PUBLIC_API_URL=http://localhost:8000
